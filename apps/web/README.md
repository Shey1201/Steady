# Steady Web

Vue 3 + TypeScript frontend for Steady.

Use the repository root commands for daily development:

```bash
npm run dev
npm run build
npm test
```

The desktop shell lives in `../desktop`, and local API handlers are mounted from `../api` during Vite development.
